apiVersion: v1
kind: Service
metadata:
  name: {{ include "stormchaser-orchestration.fullname" . }}-query
  labels:
    {{- include "stormchaser-orchestration.labels" . | nindent 4 }}
    app.kubernetes.io/component: query
spec:
  type: {{ .Values.query.service.type }}
  ports:
    - port: {{ .Values.query.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stormchaser-orchestration.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: query
